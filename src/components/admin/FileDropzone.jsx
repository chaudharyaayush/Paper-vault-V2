import { FileUp, UploadCloud } from "lucide-react";
export default function FileDropzone({file,onFile}){return <label className="block cursor-pointer"><div className="brutal-border border-dashed bg-white p-8 text-center transition hover:bg-acid">{file?<FileUp className="mx-auto mb-3" size={32}/>:<UploadCloud className="mx-auto mb-3" size={32}/>}<div className="display text-3xl">{file?file.name:"DROP PDF HERE"}</div><div className="mono mt-2 text-[10px] uppercase tracking-[0.14em]">{file?`${(file.size/1024/1024).toFixed(2)} MB · READY TO UPLOAD`:"OR CHOOSE A FILE · PDF ONLY"}</div></div><input type="file" accept="application/pdf,.pdf" className="sr-only" onChange={e=>onFile(e.target.files?.[0]||null)}/></label>}
