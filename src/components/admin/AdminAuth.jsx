import { LockKeyhole, Eye, EyeOff } from "lucide-react";
import { useState } from "react";
import Button from "../common/Button";
export default function AdminAuth({onLogin}){const [password,setPassword]=useState("");const [show,setShow]=useState(false);const [error,setError]=useState("");function submit(event){event.preventDefault();const ok=onLogin(password);if(!ok)setError("Incorrect password. Try again.")}return <div className="grid-paper min-h-[70vh] px-5 py-16 lg:px-8"><div className="mx-auto flex w-full max-w-xl items-center"><form onSubmit={submit} className="brutal-border brutal-shadow w-full bg-paper p-7 sm:p-10"><div className="mb-8 inline-flex items-center gap-3 border-[3px] border-ink bg-orange px-4 py-2"><LockKeyhole size={18}/><span className="mono text-[10px] font-bold uppercase tracking-[0.15em]">RESTRICTED AREA</span></div><h1 className="display text-7xl leading-[.82] sm:text-8xl">ADMIN<br/>ACCESS</h1><p className="mono mt-6 text-xs uppercase leading-5 text-black/60">Demo authentication is temporary. Production authentication will be handled by Supabase Auth.</p><div className="mt-8"><label className="mono mb-2 block text-[10px] font-bold uppercase tracking-[0.15em]">PASSWORD</label><div className="relative"><input className="focus-ring brutal-border w-full bg-white px-4 py-4 pr-14 font-bold outline-none" type={show?"text":"password"} value={password} onChange={e=>{setPassword(e.target.value);setError("")}} autoFocus/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-0 top-0 grid h-full w-12 place-items-center" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{error&&<div className="mono mt-3 bg-orange px-3 py-2 text-[10px] font-bold uppercase">{error}</div>}</div><Button type="submit" className="mt-6 w-full">Enter admin</Button></form></div></div>}
