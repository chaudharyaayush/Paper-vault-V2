import { Trash2, Clock3 } from "lucide-react";
import { formatDate } from "../../utils/format";
export default function UploadList({uploads,onDelete}){if(!uploads.length)return <div className="brutal-border bg-neutral-200 p-8"><div className="mono text-xs uppercase">No demo uploads yet.</div></div>;return <div className="border-t-[3px] border-ink">{uploads.map(upload=><div key={upload.id} className="grid gap-4 border-b-[3px] border-ink py-5 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><div className="display text-3xl">{upload.subjectName}</div><div className="mono mt-1 text-[9px] uppercase tracking-wider">{upload.year} · {upload.examType} · {upload.fileName} · {upload.size}</div></div><div className="mono flex items-center gap-2 text-[9px] uppercase"><Clock3 size={14}/> {formatDate(upload.uploadedAt)}</div><button onClick={()=>onDelete(upload.id)} className="focus-ring inline-flex items-center justify-center gap-2 border-[3px] border-ink bg-orange px-3 py-2 text-xs font-bold uppercase hover:bg-acid"><Trash2 size={15}/> Delete</button></div>)}</div>}
