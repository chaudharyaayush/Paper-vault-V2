export default function Marquee({items=[],tone="acid"}){const content=[...items,...items];const bg=tone==="green"?"bg-green":tone==="orange"?"bg-orange":"bg-acid";return <div className={`marquee brutal-border border-x-0 py-5 ${bg}`}><div className="marquee-track display text-5xl sm:text-7xl">{content.map((item,index)=><span key={`${item}-${index}`} className="mx-7 inline-flex items-center gap-7">{item}<span className="text-4xl">✳</span></span>)}</div></div>}
